
  if(file.exists(class.file.name)&status){
    
    voxel_beta_fit(class.file.name, 
                   voxRes, 
                   minVoxDensity, 
                   correct.topography)
  }
  
  